import aida from "../back.jpg";

const Hero = () => {
  return (
    <>
      <section className="hero">
        <img src={aida} alt="Aida ship in Hamburg" />
      </section>
    </>
  );
};

export default Hero;
